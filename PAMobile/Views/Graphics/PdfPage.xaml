<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PAMobile.Views.Graphics.PdfPage"
             Shell.TabBarIsVisible="False"
             Title="">
    <!--<ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="download.png" Command="{Binding DownloadCommand}" Order="Primary" Priority="0"/>
    </ContentPage.ToolbarItems>-->
    <Shell.TitleView>
        <ImageButton Source="download.png" Command="{Binding DownloadCommand}" HorizontalOptions="End"/>
    </Shell.TitleView>
    
    <WebView Source="{Binding PdfUrl}" x:Name="pdfViewer" />

</ContentPage>